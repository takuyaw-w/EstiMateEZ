import {aliases, mdi} from 'vuetify/iconsets/mdi-svg'

export default {
    icons: {
        defaultSet: 'mdi',
        aliases: {
            ...aliases,
        },
        sets: {
            mdi
        }
    }
}
